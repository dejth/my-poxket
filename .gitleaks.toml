title = "My Poxket public-repository secret scan"

[extend]
useDefault = true

[allowlist]
description = "Exclude local and generated content that is never committed"
paths = [
  '''(^|/)\.env$''',
  '''(^|/)\.git/''',
  '''(^|/)\.npm-cache/''',
  '''(^|/)node_modules/''',
  '''(^|/)dist/''',
]
